# Ticket Scanner App

QR-based Ticket Scanning Application for Event Management.

## Setup

1. Enter the directory: `cd ticket_scanner_app`
2. Install dependencies: `flutter pub get`
3. Run build runner (if using Envied): `dart run build_runner build`
4. Run the app: `flutter run`
